---
phase: quick-260722-wf7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/AppShell.tsx]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Gavel News nameplate stays visually centered in the header even when NavSearch expands into its input form"
    - "Gavel News / Daily Legal Brief render stacked (name on top, subtitle below), centered, not inline side by side"
    - "Gavel News font size is visibly larger than the previous single-row version"
---

<objective>
Follow-up fix to the just-merged single-row AppShell header (components/AppShell.tsx, quick task 260722-wcu): the nameplate was centered via `flex-1` between the left icon group and right utility group, so when NavSearch expands from a size-9 button into a wider input form, the right group grows and the flex-1 nameplate box shrinks, shifting the nameplate's centered content leftward. Fix by taking the nameplate out of flex flow and centering it absolutely against the full header width instead. Also: revert the nameplate from single-line "Gavel News · Daily Legal Brief" back to two stacked lines (name above, subtitle below), and increase the "Gavel News" font size slightly.
</objective>

<context>
@components/AppShell.tsx (header block, current single-row version from 260722-wcu)
@components/NavSearch.tsx (confirms the expand behavior: closed = `size-9` button, open = form with `input` at `w-28 sm:w-40 md:w-48` — this width delta is what shifts the flex-1-centered nameplate)
</context>

<tasks>

<task type="auto">
  <name>Task: Absolutely-center the nameplate and restore stacked two-line layout</name>
  <files>components/AppShell.tsx</files>
  <action>
In the header block (the single row introduced in the previous quick task):

1. Add `relative` to the outer `<header>` className so it can host an absolutely-positioned child.

2. Change the nameplate `<Link>` from `flex min-w-0 flex-1 items-center justify-center gap-1.5` (in-flow, flex-1) to an absolutely-positioned, centered element that no longer participates in the flex row's width distribution:
   `className="link-press absolute left-1/2 top-1/2 flex -translate-x-1/2 -translate-y-1/2 flex-col items-center gap-0.5"`
   This makes it center against the header's full width unconditionally — NavSearch expanding/collapsing the right-side utility group no longer affects its position, since it's removed from normal flow.

3. Restore the two-line stacked layout inside that Link (name on top, subtitle below — do not keep them inline with "·"):
   ```
   <span className="font-serif text-lg font-bold tracking-tight text-ink sm:text-xl">
     Gavel News
   </span>
   <span className="font-mono text-[9px] font-medium uppercase tracking-[0.14em] text-ink-3">
     Daily Legal Brief
   </span>
   ```
   (Font bump: was `text-base sm:text-lg`, now `text-lg sm:text-xl` — "a lil big font" per user ask. Subtitle no longer `hidden sm:inline` — show it on all breakpoints since it's now a compact second line, not extra inline width.)

4. Since the nameplate is now absolutely positioned, the header row's remaining children (left toggle buttons, right search+sign-in group) no longer need a `flex-1` spacer between them — but they DO need the right-hand utility div to stay pinned right. Add `ml-auto` back to the right-hand utility `<div className="flex shrink-0 items-center gap-2">` wrapping NavSearch + sign-in, since removing the flex-1 middle element means the left and right groups would otherwise sit adjacent with no space between them.

5. Header height: since the nameplate is two lines again, remove the fixed `h-12` from the header and instead let it size via padding, e.g. `py-1.5` (so both lines fit without clipping) — keep `px-3 sm:px-4` as-is. Do not let this grow the header back to the old two-row height; a single `py-1.5` row containing an absolutely-positioned two-line nameplate should stay compact (roughly the same total height as the two-line nameplate itself, since the left/right icon groups are shorter than that and won't force extra height).

Do not touch Sidebar.tsx, Footer.tsx, NavSearch.tsx, or any other component — only components/AppShell.tsx's header block.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Header renders with an absolutely-centered two-line "Gavel News" / "Daily Legal Brief" nameplate at a slightly larger font than before; opening NavSearch (clicking the search icon) does not visually shift the nameplate's position at all; `npx tsc --noEmit` passes with no errors; header remains compact (no return to the old two-row height).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes.
2. Visual check in browser: nameplate centered, two lines, larger "Gavel News" text; click the search icon and confirm the nameplate does not shift left.
</verification>

<output>
Create `.planning/quick/260722-wf7-center-appshell-nameplate-independent-of/260722-wf7-SUMMARY.md` when done
</output>
