---
quick_id: 260731-j0g
status: planned
---

# Quick Task 260731-j0g: Audit gavel-news-web TS/TSX/JS/CSS for bugs & readability per Google style guides; fix in place

**Created:** 2026-07-31

## Context

Full-codebase quality audit of `app/`, `components/`, and `lib/` (TypeScript,
TSX, and hand-authored CSS) against three style-guide contracts, fixing real
bugs and readability/style violations directly with Edit as they're found —
no separate findings document, this is fix-in-place.

**Style guides to apply** (read each `SKILL.md` + its `references/rules.md`
before starting the corresponding task type):
- `C:\Users\nithy\.codex\skills\google-typescript-style\` — all `.ts`/`.tsx`
- `C:\Users\nithy\.codex\skills\google-javascript-style\` — `.js`/`.mjs` config files and JS-shaped patterns inside `.ts`/`.tsx`
- `C:\Users\nithy\.codex\skills\google-html-css-style\` — `app/globals.css`, `app/system.css`, and accessibility/semantics of JSX markup only

**Boundary clarification (read before editing):** this project's markup is
JSX with Tailwind utility classes, not hand-authored HTML/CSS. Apply the
HTML/CSS guide's *semantic and accessibility* rules (real `<button>`/`<a>`
vs clickable `<div>`, `alt` text, label association, heading structure) to
JSX. Do **not** apply its *class-naming/selector* rules to Tailwind
`className` strings — renaming/restructuring Tailwind utility classes for
"semantic naming" is out of scope and would be a behavior-risking rewrite,
not a bug fix. The class-naming/selector/declaration-order rules apply only
to the two hand-authored CSS files in Task 5.

**Behavior-preservation rule:** style/readability fixes must not change
runtime behavior. Only fix an actual bug (logic error, unsafe null/undefined
handling, incorrect Server/Client boundary, unhandled promise rejection,
accessibility defect) when it is unambiguous. Do not "improve" working logic.

**Hard exclusions (do not edit, do not even open for review):**
- `node_modules/`, `.next/`
- `app/tokens.css` — byte-identical generated copy per `CLAUDE.md`, never hand-edited
- `app/api/notifications/**`, `components/NotificationCenter.tsx`,
  `supabase/migrations/20260731130000_user_notification_state.sql`, `.grok/`
  — untracked, belong to another in-flight session's unfinished feature

**Extended caution list — files with pre-existing uncommitted WIP diffs:**
`git status` shows `components/AppShell.tsx`, `components/Footer.tsx`,
`components/NavSearch.tsx`, `components/StoryReader.tsx`, `app/globals.css`,
and `app/system.css` are *already modified* in the working tree (part of the
same in-flight NotificationCenter/theme session, confirmed via `git diff`).
These files are in scope for this audit (they're not in the hard-exclusion
list above and the CSS/shell files must still get their pass), but for each
one: run `git diff <file>` **first** and treat every already-modified
line/hunk shown there as off-limits for style churn — only touch those
specific lines if there's an unambiguous bug in them, and say so explicitly
in that task's done notes. Everything outside the existing diff hunks in
these files is normal audit territory.

**Project conventions to preserve exactly (`CLAUDE.md`):**
- `lib/data/index.ts`'s `getDataSource()` env-switch pattern — do not restructure
- `lib/supabase/serviceRole.ts`'s `import "server-only"` + server-only,
  never-`NEXT_PUBLIC_`, RLS-bypass contract — the security boundary itself is
  correct as documented; only fix TS-style issues in it (e.g. the non-null
  assertion noted in Task 4), never its access-control logic
- Server Components by default; `"use client"` only on interactive leaf components

## Task 1: App Routes, Layouts & Config

Establish the baseline, then audit every route/layout file plus the small
root config files against the TypeScript/JavaScript checklist below.

- Files: `app/about/page.tsx`, `app/archive/page.tsx`,
  `app/api/search/route.ts`, `app/auth/callback/route.ts`,
  `app/auth/signin/page.tsx`, `app/calendar/page.tsx`,
  `app/edition/[date]/page.tsx`, `app/favorites/page.tsx`,
  `app/global-error.tsx`, `app/layout.tsx`, `app/not-found.tsx`,
  `app/page.tsx`, `app/search/page.tsx`, `app/settings/page.tsx`,
  `app/story/[slug]/page.tsx`, `app/story/[slug]/opengraph-image.tsx`,
  `middleware.ts`, `next.config.ts`, `vitest.config.ts`, `postcss.config.mjs`
  (excludes `app/api/notifications/**`)

- Action:
  1. **Capture baseline first, before any edit in this or later tasks:** run
     `pnpm exec tsc --noEmit` and `pnpm test` (vitest run) once, and keep the
     raw output — Task 6 needs it to separate pre-existing failures from
     anything newly introduced.
  2. For each file, apply the shared TS/JS checklist:
     - No `any` / `@ts-ignore` / `@ts-nocheck` / `@ts-expect-error` in application code.
     - No unnecessary non-null assertions (`!`) — replace with a local
       binding, early return, or control-flow narrowing that proves the
       invariant; keep an assertion only with a comment justifying why it's
       unavoidable.
     - `const`/`let` only (never `var`), one variable per declaration, strict
       `===`/`!==` (loose `== null`/`!= null` only for intentional
       null-or-undefined checks), every control-flow body braced.
     - Named function declarations for top-level functions; arrows for
       callbacks; no anonymous `function` expressions; no `.bind(this)`
       where an arrow already captures lexical `this` correctly.
     - Every `async` call, Server Action, and Route Handler
       (`app/api/search/route.ts`, `app/auth/callback/route.ts`) either
       awaits-and-catches or has an explicit `.catch`; Route Handlers return
       a proper `NextResponse`/status on error paths instead of letting an
       exception escape uncaught.
     - JSX accessibility: real `<button>`/`<a>` for interactive elements
       (not clickable `<div>`/`<span>`), meaningful `alt` on images, labels
       associated with form inputs, sensible heading order. Do not rename
       Tailwind classes (see Context boundary note).
     - Confirm Server/Client Component boundary correctness — none of these
       route/layout files should carry an unneeded `"use client"`.
     - `app/story/[slug]/opengraph-image.tsx` uses inline style objects for
       `next/og`'s `ImageResponse` (not CSS) — audit as TS/JS, not CSS; its
       existing `console.error` font-fallback log at line 131 is intentional
       diagnostics, not a bug, leave it.
     - `middleware.ts` / `next.config.ts`: confirm typed exports (`NextConfig`),
       no implicit `any`, and that `middleware.ts`/`middleware()` (not
       `proxy.ts`/`proxy()`) is still correct for the pinned Next.js 15.5.21
       per `CLAUDE.md`.
  3. Fix every unambiguous violation found directly with Edit.

- Verify: `grep -rn "@ts-ignore\|@ts-nocheck\|@ts-expect-error\|: any\b" app/about/page.tsx app/archive/page.tsx app/api/search/route.ts app/auth/callback/route.ts app/auth/signin/page.tsx app/calendar/page.tsx "app/edition/[date]/page.tsx" app/favorites/page.tsx app/global-error.tsx app/layout.tsx app/not-found.tsx app/page.tsx app/search/page.tsx app/settings/page.tsx "app/story/[slug]/page.tsx" "app/story/[slug]/opengraph-image.tsx" middleware.ts next.config.ts | grep -v '^#'` returns no matches.
- Done: baseline tsc/test output captured; all 20 files reviewed against the
  checklist; unambiguous bugs and style violations fixed; no behavior change
  beyond bug fixes.

## Task 2: Components — Shell, Navigation & Utility

- Files: `components/AppShell.tsx`, `components/Sidebar.tsx`,
  `components/NavSearch.tsx`, `components/Footer.tsx`,
  `components/ThemeToggle.tsx`, `components/CategoryFilter.tsx`,
  `components/EmptyState.tsx`, `components/SearchInput.tsx`,
  `components/SearchResults.tsx`, `components/CalendarBrowser.tsx`,
  `components/icons.tsx`
  (excludes `components/NotificationCenter.tsx`)

- Action:
  1. For `AppShell.tsx`, `Footer.tsx`, `NavSearch.tsx`: run
     `git diff components/AppShell.tsx components/Footer.tsx components/NavSearch.tsx`
     first and note which lines are already-modified WIP — leave those
     lines' styling alone per the Context caution list; only fix an
     unambiguous bug inside them, and call it out explicitly in Done below.
  2. Apply the same TS/JS + JSX-accessibility checklist as Task 1 to every
     file: no `any`/assertion misuse, braced control flow, strict equality,
     named-function/arrow discipline, real interactive elements with proper
     `aria-label`s, keyboard-operable controls, no unneeded `"use client"`
     on what could be a Server Component wrapper (verify `ThemeToggle.tsx`,
     `SearchInput.tsx` etc. genuinely need client interactivity — they're
     expected leaf client components, confirm rather than assume).
  3. `icons.tsx` (653 lines) is a large, repetitive SVG icon set — use Grep
     for `<svg` blocks to scan for missing `aria-hidden="true"`/`focusable="false"`
     on purely decorative icons and any `any`/style violations, rather than
     reading every icon top-to-bottom; only Edit where a real issue is found.
  4. Fix every unambiguous violation directly with Edit.

- Verify: `grep -rn "@ts-ignore\|@ts-nocheck\|: any\b" components/AppShell.tsx components/Sidebar.tsx components/NavSearch.tsx components/Footer.tsx components/ThemeToggle.tsx components/CategoryFilter.tsx components/EmptyState.tsx components/SearchInput.tsx components/SearchResults.tsx components/CalendarBrowser.tsx components/icons.tsx` returns no matches.
- Done: all 11 files reviewed; unambiguous bugs/style violations fixed;
  AppShell/Footer/NavSearch's pre-existing WIP diff lines left untouched
  except any unambiguous bug fixed there (list any such fix explicitly, or
  state "no fixes needed inside WIP hunks").

## Task 3: Components — Story, Content & Auth

- Files: `components/StoryReader.tsx`, `components/StoryCard.tsx`,
  `components/PYQSidebar.tsx`, `components/QuizSection.tsx`,
  `components/LegalMentorSection.tsx`, `components/LawDecodeSection.tsx`,
  `components/ExamLensSection.tsx`, `components/ExamRadarSection.tsx`,
  `components/ChallengeSection.tsx`, `components/OneLineRevisionSection.tsx`,
  `components/BeforeYouLeaveSection.tsx`, `components/VisualMemoryCard.tsx`,
  `components/RelatedStories.tsx`, `components/Markdown.tsx`,
  `components/CompleteButton.tsx`, `components/FavoriteButton.tsx`,
  `components/FavoritesList.tsx`, `components/AuthBenefits.tsx`,
  `components/SignInGate.tsx`, `components/SettingsForm.tsx`,
  `components/ShareButton.tsx`, `components/__tests__/v2-sections.test.tsx`

- Action:
  1. For `StoryReader.tsx`: run `git diff components/StoryReader.tsx` first
     and leave its already-modified WIP lines/hunks untouched per the
     Context caution list; only fix an unambiguous bug inside them and note
     it explicitly in Done below.
  2. Apply the shared TS/JS + JSX-accessibility checklist (see Task 1) to
     all files. Known concrete items to verify and fix:
     - `PYQSidebar.tsx`: `indexByPassageId.get(key)!` and `group.passage!.text`
       non-null assertions — replace with a narrowing check or documented
       invariant.
     - `StoryReader.tsx`: `root!.contains(block)` (not part of the current
       WIP diff, confirm with the git diff from step 1) — same treatment.
  3. Prioritize full reads for `StoryReader.tsx` (largest/most complex,
     1165 lines — TTS playback, click-to-seek, markdown rendering) and
     `PYQSidebar.tsx`/`SettingsForm.tsx` (form/null-handling risk). The
     smaller presentational sections (`BeforeYouLeaveSection`,
     `OneLineRevisionSection`, `VisualMemoryCard`, `RelatedStories`,
     `Markdown`) can get a lighter pass — they're short and low-complexity.
  4. `__tests__/v2-sections.test.tsx`: apply naming/style rules, but leave
     its non-null assertions (`.find(...)!`, `v2.sourcesV2!.primary`) as-is
     unless clearly wrong — asserting on guaranteed-present test fixtures is
     acceptable test-setup convention, not a violation worth churning.
  5. Fix every unambiguous violation directly with Edit.

- Verify: `grep -rln "@ts-ignore\|@ts-nocheck\|: any\b" components/StoryReader.tsx components/StoryCard.tsx components/PYQSidebar.tsx components/QuizSection.tsx components/LegalMentorSection.tsx components/LawDecodeSection.tsx components/ExamLensSection.tsx components/ExamRadarSection.tsx components/ChallengeSection.tsx components/OneLineRevisionSection.tsx components/BeforeYouLeaveSection.tsx components/VisualMemoryCard.tsx components/RelatedStories.tsx components/Markdown.tsx components/CompleteButton.tsx components/FavoriteButton.tsx components/FavoritesList.tsx components/AuthBenefits.tsx components/SignInGate.tsx components/SettingsForm.tsx components/ShareButton.tsx` returns no matches.
- Done: all 22 files reviewed; PYQSidebar and StoryReader non-null
  assertions resolved or justified; unambiguous bugs/style violations fixed;
  StoryReader's pre-existing WIP diff lines left untouched except any
  unambiguous bug fixed there (list explicitly, or state "no fixes needed
  inside WIP hunks").

## Task 4: lib/ — Data, Auth & Utilities

- Files: `lib/auth.ts`, `lib/auth-actions.ts`, `lib/auth-benefits.ts`,
  `lib/editions.ts`, `lib/format.ts`, `lib/nav.ts`, `lib/search.ts`,
  `lib/site.ts`, `lib/tts-manager.ts`, `lib/types.ts`, `lib/data/index.ts`,
  `lib/data/mock.ts`, `lib/data/supabase.ts`, `lib/mock/stories.ts`,
  `lib/supabase/client.ts`, `lib/supabase/middleware.ts`,
  `lib/supabase/server.ts`, `lib/supabase/serviceRole.ts`

- Action:
  1. Apply the TS checklist from Task 1 (no JSX/accessibility rules here).
  2. Known concrete items to resolve:
     - `lib/data/supabase.ts:36` — `type Row = Record<string, any>;` used
       throughout the file's mapper functions (`mapImportantTerm`,
       `mapCommonConfusion`, `mapHero`, etc.) to translate untyped
       Supabase/Postgres rows into typed internal shapes. This is a
       legitimate interop boundary per the style guide's own carve-out
       ("if an interop boundary requires `any`, contain it at the boundary
       and document why"). Decide case-by-case: if switching `Row` to
       `Record<string, unknown>` plus a small typed accessor is mechanical
       and does not risk behavior change, do it; otherwise keep `any` at
       this single narrow site and add a one-line comment stating it's an
       intentional, contained boundary exception for untyped Supabase rows.
       Do not spread `any` beyond this one type alias.
     - Non-null assertions to resolve with narrowing (add a local `const`
       holding the looked-up value right after the `.set`, or use
       `?? `-based fallback) instead of `!`:
       `lib/data/supabase.ts` (`editionMap.get(s.editionDate)!.push(s)`,
       `monthMap.get(key)!.editions.push(edition)`), `lib/data/mock.ts`
       (same two patterns), `lib/supabase/serviceRole.ts:22`
       (`process.env.NEXT_PUBLIC_SUPABASE_URL!`), `lib/supabase/client.ts:10-11`
       (`process.env.NEXT_PUBLIC_SUPABASE_URL!`, `...ANON_KEY!`) — for the
       env-var cases, validate presence once (throw a clear `Error` if
       missing, matching the existing pattern already used in
       `serviceRole.ts` for `SUPABASE_SERVICE_ROLE_KEY`) instead of asserting.
  3. Preserve `lib/data/index.ts`'s `getDataSource()` switch pattern exactly
     — do not restructure it, only fix genuine TS-style issues in it if any.
  4. Preserve `lib/supabase/serviceRole.ts`'s `import "server-only"` +
     server-only/RLS-bypass contract and its existing JSDoc verbatim — only
     fix the non-null assertion noted above, never its access-control logic.
  5. Fix every unambiguous violation directly with Edit.

- Verify: `grep -rn "@ts-ignore\|@ts-nocheck\|Record<string, ?any>" lib/*.ts lib/data/*.ts lib/mock/*.ts lib/supabase/*.ts | grep -v '^#'` — any remaining `Record<string, any>` hit must have an adjacent justifying comment (manually confirm); no `@ts-ignore`/`@ts-nocheck` matches at all.
- Done: all 18 files reviewed; the `Row = Record<string, any>` boundary is
  either narrowed or explicitly documented as an intentional exception; all
  five listed non-null-assertion sites are resolved or replaced with
  explicit validation; `getDataSource()` switch and `serviceRole.ts`'s
  access-control contract are unchanged in behavior.

## Task 5: CSS — globals.css & system.css

- Files: `app/globals.css`, `app/system.css` (never `app/tokens.css`)

- Action:
  1. Run `git diff app/globals.css app/system.css` first and leave the
     already-modified WIP lines/hunks untouched per the Context caution
     list; only fix an unambiguous bug there (e.g. a genuinely broken
     selector or duplicate declaration) and note it explicitly in Done.
  2. Apply the Google CSS checklist to the rest of both files:
     - One declaration per line, each terminated with `;`; a blank line
       between rule blocks; consistent, logical declaration order within a
       rule.
     - `0` without units, `.5` not `0.5`, lowercase short hex (`#fff`) where
       already hex, single quotes inside CSS string values.
     - Avoid ID selectors and unnecessary element-qualified class selectors
       (`div.foo`) — prefer bare class selectors; this is a low-risk,
       behavior-preserving rewrite only where the selector specificity
       doesn't change in a way that alters cascade order.
     - No `!important` without a documented, narrow justification. Review
       the five existing uses: `app/globals.css:579`
       (`font-size: clamp(40px, 10vw, 44px) !important`), `:595`/`:596`
       (`transition: none !important` / `animation: none !important`),
       `:749` (`transition-duration: 0.01ms !important`), and `:1041`
       (`background-color: #0f0f0f !important`). Keep any that are
       legitimate `prefers-reduced-motion`/dark-mode-override safety nets
       (a recognized narrow exception). For any that are fighting
       specificity unnecessarily, refactor the selector to remove the
       `!important` only if doing so provably doesn't change the rendered
       cascade — otherwise leave it and add a one-line comment stating why
       it's required.
  3. Fix every unambiguous violation directly with Edit.

- Verify: `grep -n "!important" app/globals.css app/system.css` — every remaining match has an adjacent comment (checked manually) or is one of the five reviewed reduced-motion/override cases; `grep -n "#[0-9a-f]\{6\}" app/globals.css app/system.css | grep -v "#[0-9a-f]\{3\}\b"` reviewed for any hex that could safely shorten to 3-digit form.
- Done: both files reviewed outside their pre-existing WIP hunks; declaration
  formatting, selector, and `!important` issues fixed or explicitly
  justified; `app/tokens.css` untouched; no rendered-output change.

## Task 6: Verification

- Files: none (read-only verification pass)
- Action: Run `pnpm exec tsc --noEmit` and `pnpm test` (vitest run) and
  compare against the baseline output captured in Task 1, step 1. Report:
  (a) any type or test failures present in both baseline and current run —
  these are pre-existing, do not attempt to fix them; (b) any new failure
  not present in the baseline — these were introduced by this audit and
  must be fixed before the task is done; (c) confirm zero new failures
  before closing out.
- Verify: `pnpm exec tsc --noEmit` and `pnpm test` both run to completion (pass or documented-pre-existing-fail) with their output captured for the comparison in Action.
- Done: tsc/test run and diffed against the Task 1 baseline; zero
  newly-introduced failures; any pre-existing failures listed separately
  with a one-line note that they predate this audit.

## Must-haves

- truths:
  - Every file listed across Tasks 1-5 (excluding the hard-exclusion list)
    has been checked against the three Google style guides; unambiguous
    bugs and style/readability violations are fixed in place.
  - No runtime behavior changed except genuine bug fixes — Tailwind classes,
    business logic, and the `DATA_SOURCE`/service-role security boundary are
    unchanged.
  - `app/tokens.css`, `app/api/notifications/**`,
    `components/NotificationCenter.tsx`, the untracked notification
    migration, and `.grok/` are untouched.
  - Pre-existing uncommitted WIP lines in `AppShell.tsx`, `Footer.tsx`,
    `NavSearch.tsx`, `StoryReader.tsx`, `globals.css`, `system.css` are left
    stylistically and behaviorally untouched (bug-fix exceptions explicitly
    noted per task).
  - `pnpm exec tsc --noEmit` and `pnpm test` show zero newly-introduced
    failures versus the Task 1 baseline.
- artifacts: none new — this is a fix-in-place audit, no new files created.
- key_links:
  - `lib/data/index.ts`'s `getDataSource()` env switch still routes to
    `lib/data/mock.ts` / `lib/data/supabase.ts` exactly as before.
  - `lib/supabase/serviceRole.ts` remains server-only
    (`import "server-only"`), never read from a client component, key never
    exposed as `NEXT_PUBLIC_`.
- explicitly out of scope: `app/tokens.css`, `node_modules/`, `.next/`,
  `app/api/notifications/**`, `components/NotificationCenter.tsx`, the
  untracked notification-state migration, `.grok/`, Tailwind
  class-naming/restructuring, any business-logic or UX change, fixing
  pre-existing (non-audit-introduced) `tsc`/`vitest` failures.
