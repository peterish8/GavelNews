---
phase: quick-260723-m87
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md, AGENTS.md]
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md and AGENTS.md both state Next.js 15.5.21 (not 15.1.4) matching package.json"
    - "CLAUDE.md and AGENTS.md both describe the Supabase project as linked/credentialed (GavelNews, ref fewdjzjkdblnvzvtjzgz) via CLI with anon URL/key in .env.local, while still stating schema/RLS/seed data have not shipped and no table should be assumed to exist"
    - "CLAUDE.md's Architecture section and AGENTS.md's Structure section both mention components/ShareButton.tsx, components/CalendarBrowser.tsx, lib/site.ts, and app/story/[slug]/opengraph-image.tsx"
    - "CLAUDE.md's GSD markers (<!-- GSD:*-start/end -->) are preserved unchanged; only content inside the Technology Stack and Architecture marked regions changes"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated Technology Stack and Architecture sections reflecting current Next.js version, Supabase link status, and new files"
    - path: "AGENTS.md"
      provides: "Updated Stack, Current State, and Structure sections matching CLAUDE.md's facts"
  key_links: []
---

<objective>
Sync CLAUDE.md and AGENTS.md with the project's actual current state after today's work: Next.js was upgraded from 15.1.4 to 15.5.21, a real Supabase project is now linked via CLI (though schema/RLS still unshipped), and several new files were added (components/ShareButton.tsx, components/CalendarBrowser.tsx, lib/site.ts, app/story/[slug]/opengraph-image.tsx). Docs-only change, no code changes.
</objective>

<context>
@CLAUDE.md
@AGENTS.md
@package.json
@lib/site.ts
@components/ShareButton.tsx
@components/CalendarBrowser.tsx
@app/story/[slug]/opengraph-image.tsx
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md Technology Stack and Architecture sections</name>
  <files>CLAUDE.md</files>
  <action>
In the `<!-- GSD:stack-start -->...<!-- GSD:stack-end -->` block: change the Framework row's Next.js version from `15.1.4` to `15.5.21` (keep the existing note about not yet being on Next 16 / middleware.ts still correct — that remains accurate). Update the "Current state" paragraph at the bottom of that block: replace the claim that only "A Supabase CLI project is linked locally (`supabase/config.toml`)" with the accurate current status — a real Supabase project ("GavelNews", ref `fewdjzjkdblnvzvtjzgz`) is linked via the CLI with anon URL/key in local `.env.local` (gitignored), but schema/RLS/seed data still have not shipped per ROADMAP.md Phase 1 — don't assume any table exists. Keep the `DATA_SOURCE=mock` sentence as-is (still accurate).

In the `<!-- GSD:architecture-start -->...<!-- GSD:architecture-end -->` block: update the `components/` bullet to include `ShareButton.tsx` (client component, Web Share API + clipboard-copy fallback) and `CalendarBrowser.tsx` alongside the existing list. Update the `lib/` bullet to include `site.ts` (`SITE_URL` constant, env-driven via `NEXT_PUBLIC_SITE_URL`, single source of truth for `metadataBase`/canonical URLs/share links) alongside the existing list. Add a note that `app/story/[slug]/opengraph-image.tsx` exists as a per-story branded OG preview image route (Next.js file-convention, edge runtime, `next/og` `ImageResponse`, fetches Source Serif 4 + IBM Plex Mono from Google Fonts at request time with a fallback to generic sans-serif on failure).

Do not touch any other GSD-marked section (Project, Conventions, Project Skills, GSD Workflow Enforcement, Developer Profile). Do not remove or alter any `<!-- GSD:*-start/end -->` comment markers.
  </action>
  <verify>
    <automated>grep -c "15.5.21" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md shows Next.js 15.5.21, an accurate Supabase-linked-but-unshipped-schema status, and updated components/lib lists including ShareButton, CalendarBrowser, site.ts, and opengraph-image.tsx; all GSD markers intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md Stack, Current State, and Structure sections</name>
  <files>AGENTS.md</files>
  <action>
In the `## Stack` section: change `Next.js 15.1.4` to `Next.js 15.5.21`.

In the `## Current State` section: replace "A Supabase CLI project is linked locally, but schema/RLS/seed data have not shipped" with the same accurate status used in CLAUDE.md — a real Supabase project ("GavelNews") is linked via CLI with anon URL/key in local `.env.local`, schema/RLS/seed data still not shipped, don't assume any table exists. Keep the mock-data sentence as-is.

In the `## Structure` section: the `components/` bullet currently just says "shared UI" — leave that generic line as-is (it's intentionally non-exhaustive) but add a one-line callout after the Structure list noting the newest additions: `components/ShareButton.tsx` (Web Share API + clipboard fallback), `components/CalendarBrowser.tsx`, `lib/site.ts` (SITE_URL constant), and `app/story/[slug]/opengraph-image.tsx` (per-story branded OG image route). Match the terse style of the rest of this file.

Do not touch `## Commands`, `## Conventions`, or `## Don't` sections — unchanged.
  </action>
  <verify>
    <automated>grep -c "15.5.21" AGENTS.md</automated>
  </verify>
  <done>AGENTS.md shows Next.js 15.5.21, accurate Supabase status matching CLAUDE.md, and mentions the new files. Commands/Conventions/Don't sections unchanged.</done>
</task>

</tasks>

<verification>
1. `grep "15.1.4" CLAUDE.md AGENTS.md` returns no matches (fully replaced).
2. `grep "15.5.21" CLAUDE.md AGENTS.md` returns matches in both files.
3. Both files mention ShareButton, CalendarBrowser, and site.ts.
4. CLAUDE.md's GSD markers are unchanged (diff shows only content lines changed, not the marker comment lines).
</verification>

<success_criteria>
- Both docs accurately reflect Next.js 15.5.21.
- Both docs accurately reflect the linked-but-unshipped Supabase state (no overstatement).
- Both docs mention the new ShareButton, CalendarBrowser, site.ts, and opengraph-image.tsx files.
- No other sections modified; CLAUDE.md GSD markers intact.
</success_criteria>

<output>
Create `.planning/quick/260723-m87-update-claude-md-and-agents-md-to-reflec/260723-m87-SUMMARY.md` when done
</output>
