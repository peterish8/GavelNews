---
status: complete
---

# Quick Task 260723-m87: Update CLAUDE.md and AGENTS.md to reflect current state

## What changed

**CLAUDE.md:**
- Technology Stack table: Next.js `15.1.4` → `15.5.21` (still on the 15.x line, Next 16 note preserved)
- Current state paragraph: replaced "Supabase CLI project is linked locally" with the accurate status — a real Supabase project ("GavelNews", ref `fewdjzjkdblnvzvtjzgz`) is linked via the CLI with anon URL/key in local `.env.local`; schema/RLS/seed data still not shipped, no table should be assumed to exist
- Architecture section: added `ShareButton.tsx` and `CalendarBrowser.tsx` to the `components/` bullet, `site.ts` to the `lib/` bullet, and a new sentence describing `app/story/[slug]/opengraph-image.tsx` as a per-story branded OG image route
- All 14 `<!-- GSD:*-start/end -->` markers preserved unchanged; only content inside the Technology Stack and Architecture regions was edited

**AGENTS.md:**
- Stack section: Next.js `15.1.4` → `15.5.21`
- Current State section: same Supabase linked-but-unshipped nuance as CLAUDE.md
- Structure section: added a one-line callout listing the same four new files

## Verification

- `grep "15.1.4"` on both files: zero matches (fully replaced)
- `grep "15.5.21"` on both files: 1 match each
- `ShareButton`/`CalendarBrowser`/`site.ts` each appear once in both files
- GSD marker count in CLAUDE.md unchanged (14, matching the original 7 sections × start/end)

Docs-only change — no code touched. Not routed through a worktree/executor agent since this was a small, mechanical, low-risk edit done directly.
