---
quick_id: 260723-kv7
mode: quick
---

# Quick Task 260723-kv7: Pull Supabase project credentials via CLI and write to .env.local

## Task

Use the already-linked Supabase CLI project (`gavel-news-web` → ref `fewdjzjkdblnvzvtjzgz`, project "GavelNews") to fetch the project URL and anon/publishable key, and write them into `.env.local` under the existing `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` slots defined in `.env.example`.

**Hard constraint (CLAUDE.md, PLAT-03):** this repo never holds the `service_role` key. Only the anon key may be written.

## Tasks

1. **Fetch credentials via Supabase CLI**
   - Files: none (read-only CLI call)
   - Action: `supabase projects api-keys --project-ref fewdjzjkdblnvzvtjzgz` to get the anon key; project URL is `https://fewdjzjkdblnvzvtjzgz.supabase.co`.
   - Verify: JSON response contains an `anon` key entry.
   - Done: URL + anon key captured.

2. **Write credentials to .env.local**
   - Files: `.env.local`
   - Action: Add/update `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` lines, preserving the existing `VERCEL_OIDC_TOKEN` line. Do NOT add `service_role`/`sb_secret_` key.
   - Verify: `.env.local` contains both vars with non-empty values, no service_role key present, file remains git-ignored (not staged).
   - Done: `.env.local` updated locally only.

## Notes

- `.env.local` is git-ignored (`.gitignore` lines 34-35) — this task produces a local-only file change, not a commit. Only PLAN.md/SUMMARY.md/STATE.md are committed.
- `DATA_SOURCE` is left as-is (still `mock` unless the user separately flips it) — out of scope for this task.
