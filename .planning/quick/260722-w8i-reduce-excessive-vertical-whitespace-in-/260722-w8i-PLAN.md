---
phase: quick-260722-w8i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/FeedView.tsx]
autonomous: true
requirements: []
must_haves:
  truths:
    - "The gap between the date-nav row and the 'Syllabus filter' heading is visibly tighter than before, without feeling cramped"
    - "The gap between the filter chips and the first story card is visibly tighter than before"
    - "No layout structure, grid columns, or component boundaries changed — only spacing utility classes"
  artifacts:
    - path: "components/FeedView.tsx"
      provides: "Homepage hero/masthead markup with reduced vertical spacing utilities"
  key_links:
    - from: "components/FeedView.tsx header element"
      to: "Syllabus filter section"
      via: "reduced mb-*/pb-* utility classes replacing the stacked mb-8+pb-8 (md:mb-10+md:pb-10) combo"
      pattern: "mb-8.*border-b.*pb-8|mb-5.*border-b.*pb-5"
---

<objective>
Tighten the excessive vertical whitespace in the homepage hero/masthead section of `components/FeedView.tsx` by reducing stacked margin+padding Tailwind utilities, without touching layout structure or logic.

Purpose: The current header stacks a bottom margin AND bottom padding before its border (mb-8/md:mb-10 + pb-8/md:pb-10), compounding into a ~64-80px gap between the date-nav row and "Syllabus filter" section below — this is the dominant contributor to the "lots of dead space" the user flagged in a screenshot. A second, smaller gap exists between the filters section and the story cards (mb-8).
Output: Updated `components/FeedView.tsx` with reduced spacing utilities; visually tighter but not cramped hero section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@components/FeedView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task: Reduce stacked vertical spacing in FeedView hero section</name>
  <files>components/FeedView.tsx</files>
  <action>
Edit only the Tailwind spacing utility classes below (lines are current as of verification; re-locate by matching content if the file has drifted slightly). Do not touch grid columns, component structure, conditional rendering, or any non-spacing classes.

1. Outer container (currently `className="mx-auto max-w-6xl px-5 py-8 md:py-12"`): reduce vertical padding to `py-6 md:py-9`.

2. Header element (currently `className="mb-8 grid gap-6 border-b border-border-app pb-8 md:mb-10 md:grid-cols-[1.4fr_auto] md:items-end md:gap-10 md:pb-10"`): this is the primary bug — it double-stacks a bottom margin (mb-8/md:mb-10) AND bottom padding (pb-8/md:pb-10) before the border, compounding into the largest visible gap. Fix by cutting both roughly in half: change `mb-8` to `mb-5`, `md:mb-10` to `md:mb-6`, `pb-8` to `pb-5`, `md:pb-10` to `md:pb-6`. Resulting class list: `mb-5 grid gap-6 border-b border-border-app pb-5 md:mb-6 md:grid-cols-[1.4fr_auto] md:items-end md:gap-10 md:pb-6`.

3. Filters section (currently `className="mb-8 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"`): reduce `mb-8` to `mb-6`.

Leave all other spacing classes in this component (mb-4 on the pills row, h1 sizing, mt-3 subtitle, mt-5 date-nav row, gap-2/gap-3 within Stat dl, etc.) untouched — they are not part of the reported issue.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>components/FeedView.tsx compiles with no type errors; outer container uses py-6 md:py-9; header uses mb-5 ... pb-5 md:mb-6 ... md:pb-6 (no more double mb-8+pb-8 stack); filters section uses mb-6 instead of mb-8; no other markup, structure, or non-spacing classes changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this is a pure client-rendered UI spacing change to an existing component with no new data flow, no user input handling, and no external dependency changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260722-01 | N/A | components/FeedView.tsx | accept | Pure Tailwind className edit, no logic/data/dependency changes — no attack surface introduced |
</threat_model>

<verification>
Run `npx tsc --noEmit` from the project root to confirm no TypeScript/JSX errors were introduced. Manually load the homepage in dev (`npm run dev`) and visually confirm the gap between the date-nav row and "Syllabus filter" heading is noticeably tighter, and the gap between filter chips and story cards is tighter, while still having breathing room (not cramped).
</verification>

<success_criteria>
- `components/FeedView.tsx` outer container, header, and filters section spacing classes updated exactly as specified
- `npx tsc --noEmit` passes with no errors
- No structural, logic, or non-spacing class changes made to the file
</success_criteria>

<output>
Create `.planning/quick/260722-w8i-reduce-excessive-vertical-whitespace-in-/260722-w8i-SUMMARY.md` when done
</output>
