---
quick_id: 260723-o65
status: complete
---

# Quick Task 260723-o65 Summary

**Description:** Fix low-contrast nav item titles in Sidebar - make them black/ink instead of muted ink-2

**Completed:** 2026-07-23

## What changed

`components/Sidebar.tsx`'s `SidebarLink`: the label span (item title —
"Today", "Calendar", "Search", "Saved", "Settings", "About") now gets an
explicit `text-ink` override when the item isn't active, instead of
inheriting the row's `text-ink-2` (muted purple-gray). Active-route styling
(white `text-on-accent` on the red `bg-brand` row) and the muted description
subtext/section labels are unchanged.

## Verification

- `pnpm exec tsc --noEmit` → no errors.
- Not visually verified in-browser (no screenshot tool available this
  session) — worth a quick look at localhost:3000's sidebar.

## Files touched

- `components/Sidebar.tsx`
