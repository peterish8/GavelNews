---
phase: quick-260723-0vx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["app/story/[slug]/opengraph-image.tsx"]
autonomous: true
requirements: []
must_haves:
  truths:
    - "OG image renders the 'Gavel News' wordmark in real Source Serif 4 bold — not the generic Satori sans-serif fallback — whenever the Google Fonts fetch succeeds"
    - "OG image renders the story headline in Source Serif 4, matching how StoryReader.tsx renders real h1 story titles on-site, instead of generic sans-serif"
    - "OG image shows a 'DAILY LEGAL BRIEF' tagline stacked beneath the wordmark in IBM Plex Mono, uppercase, letter-spaced — mirroring components/AppShell.tsx's real header lockup"
    - "If the Google Fonts fetch fails or times out, the route still returns a valid PNG (no 500, no crawler timeout) by falling back to the previous generic sans-serif fontFamily behavior"
    - "Existing red gradient background (#7f1d1d -> #dc2626 -> #b91c1c, mirroring --auth-hero-gradient) is visually unchanged"
  artifacts:
    - path: "app/story/[slug]/opengraph-image.tsx"
      provides: "Branded OG image with real Source Serif 4 + IBM Plex Mono typography, loaded at request time with graceful fallback"
      contains: "loadGoogleFont"
  key_links:
    - from: "app/story/[slug]/opengraph-image.tsx"
      to: "ImageResponse fonts option"
      via: "fonts array conditionally passed as ImageResponse's second-argument option"
      pattern: "fonts"
    - from: "app/story/[slug]/opengraph-image.tsx"
      to: "fonts.googleapis.com / fonts.gstatic.com"
      via: "fetch() calls inside loadGoogleFont, at request time in the edge route"
      pattern: "fonts.googleapis.com"
---

<objective>
Make the per-story Open Graph preview card (`app/story/[slug]/opengraph-image.tsx`) look on-brand instead of generic-AI-placeholder. Currently it renders "Gavel News" and the story headline with `fontFamily: "sans-serif"`, which Satori (next/og's renderer) resolves to a built-in generic sans — it never actually renders the site's real Source Serif 4 wordmark/headline face or IBM Plex Mono tagline face, no matter what string is passed, because Satori has no access to system/Google fonts unless font file bytes are explicitly fetched and passed via `ImageResponse`'s `fonts` option.

Purpose: the wordmark ("Gavel News" in Source Serif 4 bold + "DAILY LEGAL BRIEF" tagline in IBM Plex Mono) and the story headline (Source Serif 4) are the single biggest visual signal that separates "real Gavel News brand" from "generic AI slop" when this card is shared on social/chat previews.

Output: `app/story/[slug]/opengraph-image.tsx` fetches real Source Serif 4 (weight 700) and IBM Plex Mono (weight 500) font bytes at request time via the Google Fonts CSS2 API, passes them into `ImageResponse`'s `fonts` array, and applies them to the wordmark, tagline, headline, category kicker, and footer line — with a full fallback to the previous generic-sans behavior if the font fetch fails for any reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/story/[slug]/opengraph-image.tsx
@components/AppShell.tsx
@lib/types.ts

Real site typography this OG card must mirror (from `components/AppShell.tsx` lines ~124-134):
- Wordmark: `<span className="font-serif text-base font-bold tracking-tight text-ink ...">Gavel News</span>` — `font-serif` resolves to `"Source Serif 4", Georgia, serif` (see `app/tokens.css` `--font-serif`).
- Tagline: `<span className="font-mono text-[9px] font-medium uppercase tracking-[0.14em] text-ink-3">Daily Legal Brief</span>` — `font-mono` resolves to `"IBM Plex Mono", "Fira Code", monospace` (see `app/globals.css` line 71, `--font-mono`).

Mock story slugs available for manual verification (from `lib/mock/stories.ts`): `governor-article-200-pocket-veto`, `ipc-section-302-rare-conviction`, `data-protection-board-rules`.

<interfaces>
Reference implementation of the Google Fonts CSS2 fetch-and-parse recipe (documented Vercel/Next.js pattern — treat as ground truth, do not re-derive):

```ts
async function loadGoogleFont(fontFamily: string, weight: number, text: string) {
  const url = `https://fonts.googleapis.com/css2?family=${encodeURIComponent(fontFamily)}:wght@${weight}&text=${encodeURIComponent(text)}`;
  const css = await (
    await fetch(url, {
      headers: {
        // Old/basic User-Agent so Google's CSS2 API serves a ttf/otf src URL instead of woff2
        "User-Agent":
          "Mozilla/5.0 (Windows NT 6.1) AppleWebKit/534.34 (KHTML, like Gecko) PhantomJS/1.9.7 Safari/534.34",
      },
    })
  ).text();
  const resource = css.match(/src: url\(([^)]+)\) format\('(opentype|truetype)'\)/);
  if (resource) {
    const res = await fetch(resource[1]);
    if (res.status === 200) return await res.arrayBuffer();
  }
  throw new Error(`failed to load font data for ${fontFamily}`);
}
```

`ImageResponse`'s second argument accepts `fonts?: { name: string; data: ArrayBuffer; weight?: number; style?: "normal" | "italic" }[]`. `name` must exactly match the `fontFamily` string used inside the JSX tree for Satori to apply it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fetch real brand fonts at request time with graceful fallback</name>
  <files>app/story/[slug]/opengraph-image.tsx</files>
  <action>
Add a module-level async helper `loadGoogleFont(fontFamily: string, weight: number, text: string): Promise<ArrayBuffer>` above the `Image` component, following the reference recipe in the `<interfaces>` block above exactly, with two additions for robustness: (1) include the `text` query param (percent-encoded) on the CSS2 URL so Google returns a single, correctly-subsetted font resource — this also sidesteps the ambiguity of picking the right `@font-face` block when a family has multiple charset subsets, since the regex only grabs the first match; (2) pass `signal: AbortSignal.timeout(4000)` on both the CSS fetch and the font-binary fetch so a slow/unresponsive Google Fonts endpoint cannot hang the edge function past a few seconds. Throw (do not silently return undefined) if the regex fails to match or either fetch fails — the caller handles fallback.

In the `Image` function, after the existing `kicker`/`title` computation, build two `text` strings: one for the serif font covering everything rendered in Source Serif 4 — `` `${title} Gavel News…` `` (the ellipsis character is needed since long titles get truncated with "…") — and one for the mono font covering everything rendered in IBM Plex Mono — the fixed strings `"DAILY LEGAL BRIEF"`, `"Daily CLAT Current Affairs"`, and all four `CATEGORY_META` label values (`Constitutional Law`, `Criminal Law`, `Legal Current Affairs`, `Bare Acts Update`), concatenated, since the kicker is always one of those four fixed strings regardless of which story is requested.

Wrap two `loadGoogleFont` calls in `Promise.all` inside a `try`/`catch`: `loadGoogleFont("Source Serif 4", 700, serifText)` and `loadGoogleFont("IBM Plex Mono", 500, monoText)`. On success, build a `fonts` array: `[{ name: "Source Serif 4", data: serifData, weight: 700, style: "normal" }, { name: "IBM Plex Mono", data: monoData, weight: 500, style: "normal" }]`. On failure, `console.error` the error (include story slug for debuggability) and set `fonts` to `undefined` — this is the ONLY fallback path, no partial-fonts state.

Derive two variables consumed by the JSX (wired in Task 2): `const serifFamily = fonts ? "Source Serif 4" : "sans-serif";` and `const monoFamily = fonts ? "IBM Plex Mono" : "sans-serif";`.

Update the `ImageResponse` call's second argument (currently `{ width: 1200, height: 630 }`) to conditionally spread in `fonts`: `{ width: 1200, height: 630, ...(fonts ? { fonts } : {}) }`. Do not pass an empty/undefined `fonts` array to `ImageResponse` — omit the key entirely when the fetch failed, so Satori uses its built-in default exactly as it does today.

Do not modify the `getDataSource()` / `getStory()` data-fetching logic, the background gradient, or the outer container's width/height/padding.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`loadGoogleFont` helper exists and is called via `Promise.all` for both "Source Serif 4" (700) and "IBM Plex Mono" (500); a try/catch fully falls back to `fonts = undefined` on any failure; `ImageResponse`'s options object conditionally includes the `fonts` key only when both fonts loaded successfully; `npx tsc --noEmit` passes with zero errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply branded typography to the wordmark, tagline, headline, kicker, and footer</name>
  <files>app/story/[slug]/opengraph-image.tsx</files>
  <action>
Restructure the JSX returned from `ImageResponse` (built on top of Task 1's `serifFamily`/`monoFamily` variables) so the rendered card mirrors the real site's typography:

1. Wordmark + tagline lockup: replace the current standalone `Gavel News` div (`fontSize: 30, fontWeight: 700`) with a `flexDirection: "column"` wrapper (small `gap`, e.g. 6) containing two children: (a) `Gavel News` styled `fontFamily: serifFamily, fontSize: 34, fontWeight: 700, letterSpacing: -0.5` (mirrors AppShell's `font-serif font-bold tracking-tight`), and (b) a new `DAILY LEGAL BRIEF` div styled `fontFamily: monoFamily, fontSize: 15, fontWeight: 500, textTransform: "uppercase", letterSpacing: 2.5, opacity: 0.82` (mirrors AppShell's `font-mono ... font-medium uppercase tracking-[0.14em] text-ink-3`, adapted to white-on-red since this card has no ink-3 token available).

2. Category kicker (currently `fontSize: 22, fontWeight: 600, uppercase, letterSpacing: 2`): add `fontFamily: monoFamily` so it renders in IBM Plex Mono instead of falling through to the container's default, keeping its existing size/weight/spacing.

3. Story headline/title (currently `fontSize: 52, fontWeight: 700` with no explicit fontFamily): add `fontFamily: serifFamily`, bump `fontSize` to 56 and `lineHeight` to 1.15 — this is the single highest-visibility change since it's the largest text block on the card and directly mirrors `StoryReader.tsx`'s `font-serif` h1 treatment.

4. Footer line (`Daily CLAT Current Affairs`, currently `fontSize: 20`, no fontFamily): add `fontFamily: monoFamily`.

5. Set the outer container's `fontFamily` to `serifFamily` as the base/default (currently hardcoded `"sans-serif"`), since serif is now the dominant face; all mono elements above explicitly override it per-node.

Do not change the gradient `background`, `color: "#ffffff"`, outer `padding`/`width`/`height`, or the `justifyContent: "space-between"` structure. Do not touch Task 1's font-loading logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Wordmark renders as a stacked "Gavel News" (serif bold) + "DAILY LEGAL BRIEF" (mono uppercase) lockup; category kicker and footer line render in `monoFamily`; headline renders in `serifFamily` at 56px/1.15 line-height; gradient background and card dimensions unchanged; `npx tsc --noEmit` passes with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge route -> Google Fonts CDN | `opengraph-image.tsx` runs on the edge runtime and makes outbound `fetch()` calls to `fonts.googleapis.com` / `fonts.gstatic.com` at request time — a network dependency that did not exist before this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260723-01 | Denial of Service | `loadGoogleFont` fetch calls | mitigate | `AbortSignal.timeout(4000)` on both fetches inside `loadGoogleFont`, plus a `try`/`catch` around the `Promise.all` that fully falls back to the pre-existing generic sans-serif render — a slow or dead Google Fonts endpoint can never turn into a 500 or an unbounded hang for crawlers requesting the OG image. |
| T-quick260723-02 | Information Disclosure | `text` query param sent to Google Fonts | accept | Story titles/categories are already public content destined for a publicly-shared OG card; sending that same text to Google's font-subsetting API (as the URL `text` param) discloses nothing beyond what the OG image itself is about to make public. No auth tokens, user PII, or private story fields are ever included in the font-fetch URLs. |
| T-quick260723-03 | Tampering | URL construction in `loadGoogleFont` | accept | All dynamic values (`fontFamily`, `text`) are passed through `encodeURIComponent` before being placed in the URL query string, so story titles cannot break out of the query-string context; no new npm dependency is installed (package legitimacy gate not applicable — `fetch` is native to the edge runtime). |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors (run once after Task 1, once after Task 2).
2. Manual/visual check (unavoidable — no snapshot testing in this repo):
   - Run `pnpm dev`, then in a browser or via `curl -o og.png http://localhost:3000/story/governor-article-200-pocket-veto/opengraph-image` (or any of the other mock slugs listed in context, or `http://localhost:3000/story/does-not-exist/opengraph-image` for the "Daily CLAT Current Affairs" fallback path), open the resulting PNG.
   - Confirm: "Gavel News" renders in a real serif face (visibly different from a generic sans fallback — look for serifs on the "G" and "N"), with "DAILY LEGAL BRIEF" in a monospace uppercase line directly beneath it; the story headline below the kicker also renders in the same serif face; the red gradient background is unchanged from before.
   - Optionally run `npx next build` and confirm the `/story/[slug]/opengraph-image` route compiles without errors in the build output.
   - Force the fallback path once by temporarily breaking the font URL (e.g. typo the hostname) to confirm the route still returns a valid (if generic-sans) PNG instead of a 500 — then revert the typo.
</verification>

<success_criteria>
- `app/story/[slug]/opengraph-image.tsx` fetches Source Serif 4 (700) and IBM Plex Mono (500) at request time and passes them to `ImageResponse` via the `fonts` option.
- Wordmark, headline, kicker, tagline, and footer all use the correct branded font family, matching `components/AppShell.tsx` and `StoryReader.tsx`'s real typography choices.
- Font-fetch failure degrades gracefully to the previous generic sans-serif render — never a 500 or crash.
- No new npm dependency added; existing red gradient background unchanged.
- `npx tsc --noEmit` passes.
</success_criteria>

<output>
Create `.planning/quick/260723-0vx-make-the-per-story-og-preview-card-profe/260723-0vx-SUMMARY.md` when done
</output>
