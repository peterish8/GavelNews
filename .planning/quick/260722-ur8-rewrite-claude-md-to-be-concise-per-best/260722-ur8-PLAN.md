---
phase: quick-260722-ur8
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md, AGENTS.md]
autonomous: true
requirements: []   # quick task — not tied to a ROADMAP phase requirement ID
must_haves:
  truths:
    - "Any engineer (or agent) opening CLAUDE.md gets project purpose, actually-installed stack versions, real commands, and non-obvious gotchas in under 2 minutes of reading — no verbose research tables to wade through."
    - "CLAUDE.md's tech claims match package.json exactly (no stale Next.js 16 / shadcn / zod / next-themes / lucide-react claims for packages that aren't installed)."
    - "A non-Claude coding agent (Codex, Cursor, etc.) reading AGENTS.md gets equivalent load-bearing project context without any GSD-specific workflow instructions."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Concise, fact-checked project context for Claude Code, with GSD marker sections preserved"
      min_lines: 40
    - path: "AGENTS.md"
      provides: "Concise, tool-agnostic project context for any coding agent"
      min_lines: 30
  key_links:
    - from: "CLAUDE.md Stack section"
      to: "package.json"
      via: "version numbers must match what's actually pinned"
      pattern: "15\\.1\\.4"
---

<objective>
Rewrite `CLAUDE.md` to be concise and fact-checked against what's actually in this repo today (not what an earlier research pass assumed would be installed), and create a new `AGENTS.md` carrying the same load-bearing facts for non-Claude coding agents.

Purpose: The current `CLAUDE.md` is 138 lines dominated by "Recommended Stack / Alternatives Considered / What NOT to Use / Version Compatibility / Sources" tables from a pre-implementation research pass. It claims Next.js 16.2.x, shadcn/ui, zod, next-themes, and lucide-react — none of which match `package.json` (Next.js 15.1.4, no shadcn/zod/next-themes/lucide-react installed). Placeholder sections ("Conventions not yet established", "Architecture not yet mapped") are now fillable with real facts from the scaffolded codebase. Best-practice CLAUDE.md/AGENTS.md files are short, scannable, and high-signal — the common failure mode is an exhaustive document nobody re-reads and that silently drifts from reality.
Output: Rewritten `CLAUDE.md` (same 7 GSD marker-bounded sections, condensed content), new `AGENTS.md` (tool-agnostic, no GSD-specific instructions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@CLAUDE.md

This plan's grounding facts were verified directly against the repo (2026-07-22):
- `package.json`: Next.js 15.1.4 (not 16.2.x as the current CLAUDE.md claims), React 19.0.0, TypeScript 5.7.3, Tailwind CSS ^4.0.6, `@supabase/supabase-js` 2.49.4, `@supabase/ssr` 0.5.2, zustand 5.0.2, framer-motion 11.18.0, date-fns 4.1.0, clsx 2.1.1, Vitest 2.1.8, ESLint 9 + `eslint-config-next` 15.1.4. Package manager is pnpm (`pnpm-lock.yaml` present). `zod`, `next-themes`, `lucide-react`, and shadcn/ui are NOT installed despite being named in the current CLAUDE.md.
- `zustand` has zero imports anywhere in `lib/`, `components/`, or `app/` (grep confirmed) — installed but unused so far.
- `lib/data/index.ts` implements a `DATA_SOURCE` env-var switch (`getDataSource()`): defaults to `mockDataSource`, falls back to mock with a console warning if `DATA_SOURCE=supabase` is set before a real Supabase client is wired in.
- `.env.example` confirms this repo holds only `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` — the comment there explicitly states the service-role key must never exist in this codebase (enforces requirement PLAT-03).
- `supabase/config.toml` + `supabase/.temp/project-ref` show a Supabase CLI project is linked locally, but there are no `supabase/migrations/`, so no schema/RLS/seed data exist yet — matches ROADMAP.md Phase 1 ("Supabase Foundation & Reading-Flow Decision") being not-yet-started.
- Repo structure (verified via directory listing): `app/` has routes for `/`, `story/[slug]`, `edition/[date]`, `archive`, `search`, `favorites`, `settings`, `auth/signin`, `about`. `components/` holds ~18 shared UI components (story reader, feed view, nav/search, auth gate, favorites, settings form, theme toggle, TTS manager UI). `lib/` holds `data/`, `auth.ts`, `auth-actions.ts`, `editions.ts`, `format.ts`, `types.ts`, `tts-manager.ts`.
- `app/globals.css` confirms Tailwind v4 CSS-first config (`@import "tailwindcss"`, no `tailwind.config.js`) and that `app/tokens.css` is a "byte-identical copy" of a separate design-token source (comment in the file itself says so) — treat as generated, don't hand-edit.
- No project skills exist yet: `.claude/skills/` and `.agents/skills/` are both absent/empty in this repo.
- `CLAUDE.md` is a GSD-managed, marker-bounded file (see `<!-- GSD:{name}-start source:{file} --> ... <!-- GSD:{name}-end -->` comments already in the file). The 7 sections in order are: project, stack, conventions, architecture, skills, workflow enforcement, profile. `gsd-tools generate-claude-md` can re-sync individual sections later from their source files — **do not remove these marker comments**, only condense the prose between them. The `profile` section is managed exclusively by `generate-claude-profile` — leave it exactly as-is (still an unconfigured placeholder; this task does not touch it).

Time-sensitivity note for the executor: the facts above were verified at plan-writing time. Before writing either file, re-run a quick check of `package.json` and re-confirm no new packages were added in the interim — don't blindly copy stale version numbers if the repo has moved since this plan was written.
</context>

<target_claude_md>
Use this as the concrete target for `CLAUDE.md` (adjust only if your own re-verification in this task turns up a genuine discrepancy — e.g. a dependency version bump):

```markdown
<!-- GSD:project-start source:PROJECT.md -->
## Project

**Gavel News Web** — a free current-affairs reading site for CLAT PG aspirants. Reads published stories from Supabase, synced by a separate `gavel-news` engine repo (Python; admin + Claude Code produce daily pieces, synced via `supabase_sync.py`). Story pages are publicly readable as a teaser (headline, summary, what happened, background, sources) for SEO/shareability; deeper content (why it matters, key points, PYQ connection) plus favorite/mark-complete requires a free account (Google or email).

**Core value:** sign up in under a minute, get genuinely useful daily current-affairs content — good enough that people come back on its own merits, not because they feel tricked into signing up.

**Real strategic purpose:** every signup captures an email for a future, separate product (Gavelogy) launch list — disclosed honestly in the Terms/consent copy (DPDP Act 2023 requirement; see `.planning/PROJECT.md`).

### Constraints
- **Separate repo from the `gavel-news` engine** — no import of its Python code, only its Supabase output.
- **This repo never holds the Supabase `service_role` key** — writes to `published_stories` belong solely to the engine repo. Only the anon/publishable key lives here.
- Full product context, requirements, and phase plan: `.planning/PROJECT.md`, `.planning/ROADMAP.md`.
<!-- GSD:project-end -->

<!-- GSD:stack-start source:research/STACK.md -->
## Technology Stack

Versions below are what's actually pinned in `package.json` — check there before trusting any other doc, including this one.

| Layer | Choice | Note |
|---|---|---|
| Framework | Next.js 15.1.4, App Router | Not yet on Next 16 — `middleware.ts` is still correct; the `proxy.ts` rename only applies from Next 16 onward |
| UI | React 19.0.0 | |
| Language | TypeScript 5.7.3 | |
| Styling | Tailwind CSS 4.x, CSS-first config (`@import "tailwindcss"` in `app/globals.css`, tokens in `app/tokens.css`) | No `tailwind.config.js` — don't add one |
| Backend | Supabase (Postgres + Auth + RLS) via `@supabase/supabase-js` 2.49.4 + `@supabase/ssr` 0.5.2 | No live app wiring yet — see Current State below |
| Package manager | pnpm | Use `pnpm`, not `npm`/`yarn` (`pnpm-lock.yaml` is the lockfile) |
| Client state / misc | zustand, framer-motion, date-fns, clsx | zustand is installed but not yet used by any component — verify before assuming a store exists |
| Testing | Vitest — `pnpm test` | |
| Lint | ESLint + `eslint-config-next` (`next/core-web-vitals`) | |

**Not installed**, despite appearing in earlier drafts of this doc: `zod`, `next-themes`, `lucide-react`, shadcn/ui. Check `package.json` before importing any of these.

**Current state:** the app runs entirely on `DATA_SOURCE=mock` (`lib/data/index.ts`, 12 mock stories) — no live Supabase queries yet. A Supabase CLI project is linked locally (`supabase/config.toml`), but per `.planning/ROADMAP.md` Phase 1, schema/RLS/seed data have not shipped — don't assume any table exists.
<!-- GSD:stack-end -->

<!-- GSD:conventions-start source:CONVENTIONS.md -->
## Conventions

- All data access goes through `lib/data/index.ts`'s `getDataSource()`, switched on `process.env.DATA_SOURCE` (`mock` default). Add new data operations there, not as ad-hoc fetches in components.
- `app/tokens.css` is a byte-identical copy of a shared design-token source — treat as generated, don't hand-edit.
- Server Components by default; `"use client"` only on interactive leaf components (favorite button, theme toggle, filter chips) — public story/feed pages must stay server-rendered for SEO.
- No ORM — use `supabase-js` + CLI-generated types (`supabase gen types typescript`) once the schema exists, not Prisma/Drizzle.
<!-- GSD:conventions-end -->

<!-- GSD:architecture-start source:ARCHITECTURE.md -->
## Architecture

- `app/` — Next.js App Router routes: `/`, `story/[slug]`, `edition/[date]`, `archive`, `search`, `favorites`, `settings`, `auth/signin`, `about`.
- `components/` — shared UI: story reader, feed view, nav/search, auth gate, favorites, settings form, theme toggle.
- `lib/` — `data/` (mock source + env switch), `auth.ts` / `auth-actions.ts`, `editions.ts`, `format.ts`, `types.ts`, `tts-manager.ts`.
- `supabase/` — CLI-linked project, no migrations yet (Phase 1 builds schema/RLS).
<!-- GSD:architecture-end -->

<!-- GSD:skills-start source:skills/ -->
## Project Skills

No project skills found. Add skills to any of: `.claude/skills/`, `.agents/skills/`, `.cursor/skills/`, `.github/skills/`, or `.codex/skills/` with a `SKILL.md` index file.
<!-- GSD:skills-end -->

<!-- GSD:workflow-start source:GSD defaults -->
## GSD Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

Use these entry points:
- `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
- `/gsd-debug` for investigation and bug fixing
- `/gsd-execute-phase` for planned phase work

Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.
<!-- GSD:workflow-end -->

<!-- GSD:profile-start -->
## Developer Profile

> Profile not yet configured. Run `/gsd-profile-user` to generate your developer profile.
> This section is managed by `generate-claude-profile` -- do not edit manually.
<!-- GSD:profile-end -->
```
</target_claude_md>

<target_agents_md>
Use this as the concrete target for `AGENTS.md`:

```markdown
# AGENTS.md

Project context for AI coding agents (Codex, Cursor, Gemini CLI, etc.). Claude Code should follow `CLAUDE.md` instead, which adds GSD-specific workflow requirements this file intentionally omits.

## Project

Gavel News Web — a free current-affairs reading site for CLAT PG aspirants. Reads published stories from Supabase, synced by a separate `gavel-news` engine repo. Story pages are publicly readable as a teaser (headline, summary, what happened, background, sources); deeper content (why it matters, key points, PYQ connection) plus favorite/mark-complete requires a free account.

This repo never holds the Supabase `service_role` key — writes to `published_stories` belong solely to the sibling engine repo.

## Stack

Check `package.json` for exact versions — it is the source of truth, not this file.

- Next.js 15.1.4 (App Router), React 19.0.0, TypeScript 5.7.3
- Tailwind CSS 4.x, CSS-first config — no `tailwind.config.js`
- Supabase (`@supabase/supabase-js` + `@supabase/ssr`) for Postgres + Auth + RLS — not yet wired into the app (see Current State)
- pnpm package manager
- Vitest for tests, ESLint (`next/core-web-vitals`) for lint

## Current State

The app currently runs entirely on mock data (`DATA_SOURCE=mock`, see `lib/data/index.ts`) — there is no live Supabase querying yet. A Supabase CLI project is linked locally, but schema/RLS/seed data have not shipped (`.planning/ROADMAP.md` Phase 1). Don't assume any Supabase table exists.

## Commands

- `pnpm dev` — dev server
- `pnpm build` / `pnpm start` — production build/serve
- `pnpm lint` — ESLint
- `pnpm typecheck` — `tsc --noEmit`
- `pnpm test` — Vitest

## Structure

- `app/` — App Router routes
- `components/` — shared UI
- `lib/` — data access, auth, formatting, types
- `supabase/` — CLI project (linked, no migrations yet)
- `.planning/` — project/requirements/roadmap docs (read `PROJECT.md` and `ROADMAP.md` for full product context)

## Conventions

- All data access goes through `lib/data/index.ts`'s `getDataSource()` — don't fetch data ad-hoc in components.
- Server Components by default; `"use client"` only on interactive leaf components. Public story/feed pages must stay server-rendered (SEO is a stated requirement).
- `app/tokens.css` is a generated copy of shared design tokens — don't hand-edit.

## Don't

- Don't add a Prisma/Drizzle ORM layer — use `supabase-js` + CLI-generated types.
- Don't add ads, a quiz/mastery feature, revision/planner/analytics, or UG/PG split content — explicitly out of scope for v1.
- Don't put a Supabase `service_role` key anywhere in this repo, including as an empty placeholder.
- Don't assume `zod`, `next-themes`, `lucide-react`, or shadcn/ui are installed — they aren't yet; check `package.json`.
```
</target_agents_md>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md to be concise and fact-checked</name>
  <files>CLAUDE.md</files>
  <action>
Read the current `CLAUDE.md`, `package.json`, `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` to re-confirm the grounding facts listed in the context section above still hold (package versions, Supabase project state, mock-data default). If any fact has changed since this plan was written, update the target content accordingly rather than copying it blindly. Then overwrite `CLAUDE.md` with the content in `<target_claude_md>` above (or your corrected version). Preserve all 7 `<!-- GSD:{name}-start -->` / `<!-- GSD:{name}-end -->` marker comment pairs exactly as they appear in the target — these are read by `gsd-tools generate-claude-md` for future targeted resync and must not be removed, renamed, or reordered. Do not touch `.planning/research/STACK.md` or any other `.planning/` source file — this task only rewrites the rendered `CLAUDE.md`.
  </action>
  <verify>
    <automated>grep -q "15.1.4" CLAUDE.md && ! grep -q "Alternatives Considered" CLAUDE.md && [ "$(grep -c 'GSD:.*-start' CLAUDE.md)" -eq 7 ] && [ "$(grep -c 'GSD:.*-end' CLAUDE.md)" -eq 7 ] && [ "$(wc -l < CLAUDE.md)" -lt 95 ] && echo PASS</automated>
  </verify>
  <done>CLAUDE.md is under ~95 lines, contains no "Alternatives Considered"/"Version Compatibility"/"Sources" tables, states Next.js 15.1.4 (matching package.json), and retains all 7 GSD marker section pairs in their original order.</done>
</task>

<task type="auto">
  <name>Task 2: Create AGENTS.md for tool-agnostic agent context</name>
  <files>AGENTS.md</files>
  <action>
Create `AGENTS.md` at the repo root using the content in `<target_agents_md>` above (adjusted only for any facts you corrected in Task 1). This file must NOT contain any GSD-specific marker comments (`<!-- GSD:... -->`) or the "GSD Workflow Enforcement" section — those are Claude Code/GSD tooling concerns that stay exclusive to `CLAUDE.md`. Keep it short: project purpose, stack (with a pointer to `package.json` as source of truth), current state, commands, structure, conventions, and a "Don't" list of explicit anti-patterns.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -q "pnpm dev" AGENTS.md && ! grep -q "GSD:" AGENTS.md && ! grep -qi "GSD Workflow Enforcement" AGENTS.md && [ "$(wc -l < AGENTS.md)" -lt 70 ] && echo PASS</automated>
  </verify>
  <done>AGENTS.md exists at repo root, under ~70 lines, contains project/stack/commands/structure/conventions/don't-do sections, and has zero GSD-specific content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan only edits two Markdown documentation files (`CLAUDE.md`, `AGENTS.md`). No code, config, auth, or data-access surface is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260722-01 | Information Disclosure | CLAUDE.md / AGENTS.md content | accept | Both files are already public-repo documentation with no secrets; content only describes stack/structure facts already visible in `package.json` and the file tree. Executor must not paste any `.env` values or real Supabase project refs into either file. |
</threat_model>

<verification>
1. `git diff CLAUDE.md` shows the verbose tables removed and stack facts matching `package.json`.
2. `cat AGENTS.md` confirms a short, tool-agnostic file with no GSD markers.
3. Both automated `<verify>` commands in the tasks above return `PASS`.
</verification>

<success_criteria>
- CLAUDE.md is condensed (all 7 GSD marker sections preserved, verbose research tables removed) and every stack claim matches `package.json`.
- AGENTS.md exists, is short, and carries equivalent load-bearing facts without GSD-specific instructions.
- Neither file references packages that aren't actually installed (zod, next-themes, lucide-react, shadcn/ui) without flagging them as not-yet-installed.
</success_criteria>

<output>
Create `.planning/quick/260722-ur8-rewrite-claude-md-to-be-concise-per-best/260722-ur8-SUMMARY.md` when done
</output>
